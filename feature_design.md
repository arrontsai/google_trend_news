# 台股晨報 - 標的漲跌趨勢功能規劃

此文件規劃如何在 LINE 機器人的回覆中加入 AI 分析提到的標的最近漲跌幅。

## 1. 目標

在分析報告的末尾，針對 AI 提到的「標的關鍵字」，自動附加其實時或最近一個交易日的漲跌數據，增加資訊的參考價值。

## 2. 數據來源規劃

- **台股標的 (Taiwan Stocks):** 使用 **Yahoo Finance (yfinance)**。
  - 原因：穩定、格式標準、支援 `.TW` 與 `.TWO` 後綴。
- **國際資產/美股 (Global Assets):** 優先考慮 **Yahoo Finance**。
  - 原因：可統一套件處理，且支援多數美股與指數（如 `TSLA`, `^NDX`）。
  - 備選：Perplexity Finance API (若需要更深入的即時數據洞察，但目前 Yahoo Finance 已足夠顯示漲跌幅)。

## 3. 實作流程

### Step 1: 標的提取

利用正規表達式 (Regex) 從 AI 生成的摘要中提取標的代碼。
格式預計為：`公司名(代碼)` -> 例如 `台積電(2330)`。

### Step 2: 價格查詢

針對提取出的代碼進行轉換：

- 純數字（如 `2330`）-> 判斷為台股，嘗試 `2330.TW` 或 `2330.TWO`。
- 字母（如 `TSLA`）-> 直接查詢。

### Step 3: 格式化輸出

在原本的分析內容下方新增：

```text
📈 標的動態回顧：
- 2330 台積電: $1050 (+2.4%)
- TSLA 特斯拉: $250.2 (-1.2%)
- ^TWII 加權指數: 23,210 (+0.8%)
```

## 4. 預期效果

用戶在看完 AI 整理的產業洞察後，能立刻掌握相關個股的走勢，減少手動切換 App 查看市價的需求。
